{
  "name": "shinjuku-dooh-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.js",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
